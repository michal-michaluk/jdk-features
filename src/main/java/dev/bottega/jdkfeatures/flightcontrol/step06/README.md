# Step 06 — JEP 441 Pattern-Matching switch (instruction)

> Wzorzec: ten pakiet niesie **rozwiązanie kroku 5** (kod + testy) oraz instrukcję
> kroku 06. / *Reference solution for step 5 lands here; this file holds the task.* /

## Zadanie
Klasyfikacja przez switch z type patterns + when (np. alarm gdy speed>X i heading w sektorze), obsługa case null.

## Akceptacja
Każdy guard, null, wyczerpujący sealed switch.
